import React from "react";
const Home = () => {
  return <a href="/"></a>;
};

const About = () => {
  return <a href="/About/"></a>;
};

const hospital = () => {
  return (
    <div>
      <h1>NAME of the hospital</h1>
      <form>
        <input type={text} placeholder="ROOM or DEPARTMENT"></input>
        <input type={SubmitEvent} onClick={(href = "/maps/")}></input>
      </form>
    </div>
  );
};
